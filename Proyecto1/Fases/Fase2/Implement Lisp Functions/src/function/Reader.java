package function;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

public class Reader {
	public void caseReader(String scan) {
			
	}
	private String getCase(String scan) {
		if (evaluate("^[((][]"), scan) {
			return "function";
		}
	}

}
